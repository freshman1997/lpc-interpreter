fun churn(n) {
  int s = 0;
  int i = 0;
  while (i < n) {
    var arr = ({i, i + 1, i + 2, i + 3, i + 4, i + 5});
    var map = ([i: arr, i + 10: ({i, i + 1, i + 2})]);
    s += arr[0];
    s += i;
    i += 1;
  }
  return s;
}

fun main() {
  return churn(20000);
}
