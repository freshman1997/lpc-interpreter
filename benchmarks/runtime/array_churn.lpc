int main() {
    int n = 100000;
    int i = 0;
    int sum = 0;
    while (i < n) {
        var arr = ({ i, i + 1, i + 2, i + 3 });
        sum = sum + arr[0];
        i = i + 1;
    }
    if (sum <= 0) return 1;
    return sum;
}
