fun main() {
    int n = 500000;
    int i = 0;
    int sum = 0;
    var offset = 10;
    var fn = fun(a) {
        offset = offset + a;
        return offset;
    };
    while (i < n) {
        sum = sum + fn(i);
        i = i + 1;
    }
    if (sum <= 0) return 1;
    return sum;
}
