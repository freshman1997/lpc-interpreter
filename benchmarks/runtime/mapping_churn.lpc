int main() {
    int n = 100000;
    int i = 0;
    int sum = 0;
    while (i < n) {
        mapping m = ([ i : i + 1, i + 100 : i + 2 ]);
        sum = sum + m[i];
        i = i + 1;
    }
    if (sum <= 0) return 1;
    return sum;
}
